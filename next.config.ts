import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['takeout-dev.skylark.co.jp', 'app-cms-storage.skylark-app.net', 'https://takeout.skylark.co.jp'],
  },
};

export default nextConfig;
